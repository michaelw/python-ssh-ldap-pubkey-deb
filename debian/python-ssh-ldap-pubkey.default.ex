# Defaults for python-ssh-ldap-pubkey initscript
# sourced by /etc/init.d/python-ssh-ldap-pubkey
# installed at /etc/default/python-ssh-ldap-pubkey by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
